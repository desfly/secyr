{
  "build": "0057",
  "project": "HomeGuard-S3",
  "date": "2026-08-12",
  "focus": "prevent administrator lockout and make access persistence transactional",
  "security": {
    "invariant": "at least one enabled Admin must remain after every managed user mutation",
    "rejection": "409 last_admin_required",
    "access_capacity": 8,
    "ram_nvs_transactional_rollback": true
  },
  "behavior": {
    "disable_only_admin": "rejected",
    "demote_only_admin_to_user": "rejected",
    "demote_only_admin_to_guest": "rejected",
    "add_second_admin": "allowed",
    "demote_one_of_two_admins": "allowed",
    "persistence_failure": "restore exact pre-mutation AccessControl state in RAM"
  },
  "api": {
    "user_list_adds": "enabledAdmins",
    "successful_user_set_adds": "enabledAdmins"
  },
  "tests": [
    "tests/test_build0057.cpp"
  ]
}
