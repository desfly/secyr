{
  "build": "0058",
  "project": "HomeGuard-S3",
  "date": "2026-08-12",
  "focus": "centralized PIN brute-force throttling for every AccessControl caller",
  "security": {
    "clock": "ESP-IDF monotonic esp_timer",
    "scope": ["login", "HTTP control APIs", "command router", "MQTT callers using AccessControl"],
    "unknown_user_bucket": "shared",
    "max_backoff_ms": 30000,
    "account_enumeration_login_response": "invalid_credentials"
  },
  "backoff_ms": [250, 500, 1000, 2000, 5000, 10000, 30000],
  "dos_behavior": {
    "attempt_during_block": "DeniedRateLimited without extending blocked_until",
    "correct_pin_after_expiry": "clears failure history",
    "correct_pin_wrong_role": "clears credential-failure history then returns DeniedRole"
  },
  "tests": [
    "tests/test_build0058.cpp",
    "tests/esp-idf-mock/include/esp_timer.h"
  ]
}
